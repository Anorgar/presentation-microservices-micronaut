server:
  port: 8000

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Anorgar/presentation-microservices-micronaut
          cloneOnStart: true
          skipSslValidation: true
          searchPaths: configuration*